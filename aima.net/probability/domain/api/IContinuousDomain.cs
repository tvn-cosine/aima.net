namespace aima.net.probability.domain.api
{
    /// <summary>
    /// A Domain over a continuous not countable set of objects (e.g. the Real numbers).
    /// </summary>
    public interface IContinuousDomain : IDomain
    {

    } 
}
